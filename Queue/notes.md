--> Short Summary Deque
=> Deque = double ended queue
=> insert + delete → both ends
=> types → input/output restricted
=> uses → sliding window, scheduling, undo/redo
=> implementation → array (circular) + doubly linked list